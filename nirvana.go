package main

import (
//"bytes"
//"encoding/json"
//"net/http"
//"strconv"
//"time"
)

func SendToNirvana(ipaddr string, success bool) error {
	/* connect to nervana */
	return nil
}

func getSLAFlag(ipaddr string) string {
	/* Connect to score server */
	flag := "**********"
	return flag
}
